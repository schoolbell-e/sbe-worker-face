import { BlobImageDirective } from './blob-image.directive';

describe('BlobImageDirective', () => {
  it('should create an instance', () => {
    const directive = new BlobImageDirective();
    expect(directive).toBeTruthy();
  });
});
